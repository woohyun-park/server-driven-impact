---
'@server-driven-impact/core': minor
'@server-driven-impact/cache-contract': minor
'@server-driven-impact/runtime': minor
'@server-driven-impact/postgres': minor
'@server-driven-impact/sqlite': minor
'@server-driven-impact/tanstack-query': minor
---

Add versioned server-owned cache contracts, runtime invalidation responses, OpenAPI metadata, and a TanStack Query executor while preserving logical ImpactSet v1.

Support recursive declarative keys and input normalization, separate infinite page input, validate endpoint coverage with explicit exclusions, explain conservative widening, and preserve committed business data across both failure phases. Add opt-in cancellation of initial reads and distinct refetch error handling. Ship the six-package set as an identifiable cache-contract prerelease with matching dependency ranges and integrity manifests.
