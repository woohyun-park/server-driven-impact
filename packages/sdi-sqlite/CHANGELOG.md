# Changelog

## 0.5.0-impact.0

- Remove cache-specific string-comparison proof plumbing from the adapter contract.

## 0.4.0

- Expose synchronous prepare().run/get/all with native results and context lifetime checks.
- Preserve unrelated resource detail on collector overflow, switch observers for shared managed engines, and forbid direct collector access.
- Capture certified fixed-filter values for conservative automatic row exclusion; require the matching 0.4 core/runtime group.

## 0.1.1

- Move package source, documentation, and releases to the standalone Server-Driven Impact repository.

## 0.1.0

- Initial public release of the SQLite adapter.
